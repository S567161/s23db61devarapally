extends layout

block content
  h1= title
  p Welcome to  Udaykiranreddy #{title} page
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/Wolf") Take me to Wolf
    br
    a(href="/logout") Logout
